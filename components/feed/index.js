import styles from './index.module.scss';


const Feed = props => {

    return (
        <div className={styles.Feed}>
            <span>Feed here.</span>
        </div>
    );
};

export default Feed;
